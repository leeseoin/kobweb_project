# 🔧 WebSocket 채팅 시스템 디버깅 보고서
**작성일**: 2025년 10월 15일
**프로젝트**: KOB Backend - Spring Boot WebSocket Chat

---

## 📋 목차
1. [문제 개요](#문제-개요)
2. [주요 발견 사항](#주요-발견-사항)
3. [문제 1: 메시지 DB 저장 실패](#문제-1-메시지-db-저장-실패)
4. [문제 2: RabbitMQ STOMP Destination 에러](#문제-2-rabbitmq-stomp-destination-에러)
5. [최종 해결 방안](#최종-해결-방안)
6. [학습 내용](#학습-내용)

---

## 문제 개요

### 증상
- WebSocket을 통해 채팅 메시지 전송 시 실시간으로는 표시되나 **DB에 저장되지 않음**
- SimpleBroker 사용 시 정상 작동하나 **RabbitMQ STOMP Broker로 전환 시 에러 발생**
- 에러 메시지: `'/chat/{roomId}' is not a valid topic destination`

### 영향 범위
- 채팅 메시지 영구 저장 불가
- RabbitMQ 기반 scale-out 구조 사용 불가
- 실시간 채팅 기능 일부 작동 중단

---

## 주요 발견 사항

### 1. 트랜잭션 관리 문제
- **원인**: Neo4j와 JPA(PostgreSQL) 동시 사용 시 TransactionManager 충돌
- **증상**: `@Transactional` 어노테이션이 작동하지 않음
- **영향**: Hibernate INSERT 쿼리가 생성되지 않음

### 2. RabbitMQ STOMP 엄격한 Destination 검증
- **원인**: RabbitMQ는 SimpleBroker보다 destination 패턴 검증이 엄격
- **증상**: `/topic/chat/{roomId}` 패턴을 거부
- **영향**: 채팅방 구독 실패, 메시지 수신 불가

---

## 문제 1: 메시지 DB 저장 실패

### 🔍 문제 진단 과정

#### 1단계: 초기 증상 확인
```
로그: "메시지 저장 완료"
DB 쿼리: SELECT COUNT(*) FROM chat_messages → 0 rows
Hibernate 로그: INSERT 쿼리 없음
```

#### 2단계: 원인 탐색
1. `@Transactional` 추가 시도 → **실패**
2. `saveAndFlush()` 사용 시도 → **실패** (TransactionRequiredException)
3. `TransactionTemplate` 사용 시도 → **실패**
4. EntityManager 주입 후 `isJoinedToTransaction()` 체크 → **NO**

#### 3단계: 근본 원인 발견

**파일 위치**: `Neo4jConfig.java:14`

```java
@Configuration
@EnableNeo4jRepositories(basePackages = "com.kob_backend_seoin.kob_backend.repository.neo4j")
@EnableTransactionManagement  // ← 🚨 문제 원인!
public class Neo4jConfig extends AbstractNeo4jConfig {
```

**문제점**:
- `@EnableTransactionManagement`가 **Neo4j TransactionManager를 기본 TransactionManager로 설정**
- JPA TransactionManager가 무시됨
- JPA 트랜잭션이 전혀 작동하지 않음

### ✅ 해결 방법

#### 1. JPA TransactionManager 명시적 설정

**새 파일 생성**: `JpaConfig.java`

```java
@Configuration
@EnableTransactionManagement
@EnableJpaRepositories(
    basePackages = "com.kob_backend_seoin.kob_backend.repository",
    excludeFilters = @ComponentScan.Filter(
        type = FilterType.REGEX,
        pattern = "com\\.kob_backend_seoin\\.kob_backend\\.repository\\.neo4j\\..*"
    ),
    transactionManagerRef = "jpaTransactionManager"
)
public class JpaConfig {

    @Primary
    @Bean(name = "jpaTransactionManager")
    public PlatformTransactionManager jpaTransactionManager(EntityManagerFactory entityManagerFactory) {
        return new JpaTransactionManager(entityManagerFactory);
    }
}
```

**핵심 포인트**:
- `@Primary`: JPA TransactionManager를 **기본 TransactionManager**로 지정
- `excludeFilters`: Neo4j Repository 제외
- `transactionManagerRef`: JPA Repository와 JPA TransactionManager 명시적 연결

#### 2. Neo4j TransactionManager 분리

**수정 파일**: `Neo4jConfig.java`

```java
@Configuration
@EnableNeo4jRepositories(
    basePackages = "com.kob_backend_seoin.kob_backend.repository.neo4j",
    transactionManagerRef = "neo4jTransactionManager"
)
public class Neo4jConfig extends AbstractNeo4jConfig {

    @Bean(name = "neo4jTransactionManager")
    @Override
    public PlatformTransactionManager transactionManager(
            Driver driver,
            DatabaseSelectionProvider databaseSelectionProvider) {
        return new Neo4jTransactionManager(driver, databaseSelectionProvider);
    }
}
```

**핵심 포인트**:
- `@EnableTransactionManagement` 제거 (JpaConfig에서 관리)
- `transactionManagerRef` 명시
- `transactionManager()` 메서드 오버라이드

#### 3. Service Layer 트랜잭션 지정

**수정 파일**: `ChatMessageService.java`

```java
@Service
public class ChatMessageService {

    @Transactional(transactionManager = "jpaTransactionManager")
    public ChatMessage saveMessage(...) {
        // 메시지 저장 로직
    }
}
```

### 📊 결과

**Before**:
```
💾 save() 호출 시작...
✅ save() 완료
=== 메시지 저장 완료 ===
(Hibernate INSERT 쿼리 없음)
```

**After**:
```
💾 save() 호출 시작...
✅ save() 완료
Hibernate:
    insert into chat_messages
        (chat_room_id, client_message_id, content, sender_id, sent_at, sequence, id)
    values
        (?, ?, ?, ?, ?, ?, ?)
✅ 메시지 저장 완료
```

---

## 문제 2: RabbitMQ STOMP Destination 에러

### 🔍 문제 진단 과정

#### 증상
```
ERROR: Invalid destination
body: '/chat/b522cb61-ce4f-454a-a2b4-9a6a8848b920' is not a valid topic destination
```

#### SimpleBroker vs RabbitMQ 비교

| 항목 | SimpleBroker | RabbitMQ STOMP |
|------|-------------|----------------|
| Destination 검증 | 느슨함 | **엄격함** |
| `/topic/chat/{roomId}` | ✅ 허용 | ❌ 거부 |
| Routing 방식 | 단순 매칭 | **Exchange + Routing Key** |

#### RabbitMQ의 Destination 파싱 방식

```
입력: /topic/chat/b522cb61-ce4f-454a-a2b4-9a6a8848b920

RabbitMQ 해석:
1. /topic/ → amq.topic exchange
2. chat/b522cb61-... → routing key
   ↑ 문제: "chat/"를 또 다른 prefix로 오해!
```

**RabbitMQ는 `/topic/` 이후에 추가 슬래시(`/`)가 있으면 혼란스러워함**

### ✅ 해결 방법

#### Destination 패턴 변경

**수정 파일**: `WebSocketChatController.java`

**수정 위치 1**: Line 495
```java
// ❌ Before
String topicDestination = "/topic/chat/" + chatRoom.getId().toString();

// ✅ After (RabbitMQ 호환 패턴)
String topicDestination = "/topic/rooms." + chatRoom.getId().toString();
```

**수정 위치 2**: Line 547
```java
// ❌ Before
String topicDestination = "/topic/chat/" + chatRoom.getId().toString();

// ✅ After (RabbitMQ 호환 패턴)
String topicDestination = "/topic/rooms." + chatRoom.getId().toString();
```

#### 패턴 선택 이유

| 패턴 | RabbitMQ 해석 | 장점 |
|------|--------------|------|
| `/topic/chat/{id}` | ❌ chat/id → 혼란 | - |
| `/topic/{id}` | ✅ id → 단순 | 간단함 |
| `/topic/rooms.{id}` | ✅ rooms.id → 명확 | **의미 명확, routing key 패턴에 적합** |

**선택**: `/topic/rooms.{id}` - 더 명확하고 RabbitMQ의 routing key 패턴과 잘 맞음

### 📊 결과

**Before (SimpleBroker)**:
```
구독: /topic/chat/1f63367a-99f0-44b5-a491-4dcb2cc2d144
상태: ✅ 작동 (SimpleBroker는 허용)
```

**Before (RabbitMQ)**:
```
구독: /topic/chat/1f63367a-99f0-44b5-a491-4dcb2cc2d144
상태: ❌ ERROR - Invalid destination
```

**After (RabbitMQ)**:
```
구독: /topic/rooms.1f63367a-99f0-44b5-a491-4dcb2cc2d144
상태: ✅ 정상 작동
브로드캐스트: ✅ 성공
```

---

## 최종 해결 방안

### 백엔드 변경 사항

#### 1. 새로운 Config 파일
- **파일**: `JpaConfig.java`
- **목적**: JPA TransactionManager를 Primary로 설정
- **핵심**: `@Primary @Bean(name = "jpaTransactionManager")`

#### 2. 기존 Config 수정
- **파일**: `Neo4jConfig.java`
- **변경**: `@EnableTransactionManagement` 제거, TransactionManager 분리
- **핵심**: `transactionManagerRef = "neo4jTransactionManager"`

#### 3. Service Layer 수정
- **파일**: `ChatMessageService.java`
- **변경**: `@Transactional(transactionManager = "jpaTransactionManager")` 명시
- **이유**: 어느 TransactionManager를 사용할지 명확히 지정

#### 4. WebSocket Controller 수정
- **파일**: `WebSocketChatController.java`
- **변경**: Destination 패턴 `/topic/chat/{id}` → `/topic/rooms.{id}`
- **위치**: Line 495, Line 547

### 프론트엔드 변경 필요 사항

**파일**: `useWebSocket.ts` (또는 WebSocket 구독 코드)

```typescript
// ❌ 기존 코드
const destination = `/topic/chat/${roomId}`;

// ✅ 새로운 코드
const destination = `/topic/rooms.${roomId}`;
```

**변경 위치**:
- 채팅방 구독 함수
- `subscribeToRoom()` 또는 유사 함수

---

## 학습 내용

### 1. Spring 다중 데이터소스 환경에서의 TransactionManager

**핵심 교훈**:
- 여러 데이터소스(JPA, Neo4j) 사용 시 **TransactionManager를 명시적으로 분리**해야 함
- `@Primary`를 사용하여 **기본 TransactionManager 지정** 필수
- `@EnableTransactionManagement`는 **한 곳에서만** 선언

**Best Practice**:
```java
// JPA Config
@Primary
@Bean(name = "jpaTransactionManager")
public PlatformTransactionManager jpaTransactionManager(...) { ... }

// Neo4j Config
@Bean(name = "neo4jTransactionManager")
public PlatformTransactionManager neo4jTransactionManager(...) { ... }

// Service Layer
@Transactional(transactionManager = "jpaTransactionManager")
public void saveToPostgreSQL() { ... }

@Transactional(transactionManager = "neo4jTransactionManager")
public void saveToNeo4j() { ... }
```

### 2. RabbitMQ STOMP vs SimpleBroker

**SimpleBroker**:
- ✅ 개발/테스트에 적합
- ✅ 설정 간단
- ❌ Scale-out 불가
- ❌ Destination 검증 느슨

**RabbitMQ STOMP**:
- ✅ Scale-out 지원
- ✅ 고성능, 안정성
- ⚠️ Destination 검증 엄격
- ⚠️ Routing key 패턴 이해 필요

**Destination 네이밍 규칙**:
```
✅ 권장: /topic/entity.id
   예: /topic/rooms.123, /topic/users.456

❌ 피해야 함: /topic/entity/id
   예: /topic/chat/123 (RabbitMQ에서 혼란)
```

### 3. WebSocket 디버깅 체크리스트

#### 메시지 저장 안 될 때
1. ✅ Hibernate INSERT 쿼리 로그 확인
2. ✅ `isJoinedToTransaction()` 체크
3. ✅ TransactionManager 충돌 확인
4. ✅ 다중 데이터소스 환경인지 확인

#### RabbitMQ Destination 에러 발생 시
1. ✅ Destination 패턴에 슬래시(`/`) 중복 사용 확인
2. ✅ SimpleBroker로 전환 후 테스트
3. ✅ RabbitMQ 로그 확인
4. ✅ Routing key 패턴 검토

---

## 결론

### 성과
- ✅ 메시지 DB 저장 문제 해결 (TransactionManager 분리)
- ✅ RabbitMQ STOMP 호환성 확보 (Destination 패턴 변경)
- ✅ Scale-out 가능한 채팅 시스템 구축 완료

### 남은 작업
- ⏳ 프론트엔드 Destination 패턴 수정 필요
- ⏳ WebSocket 인증 Principal null 문제 해결 (낮은 우선순위)
- ⏳ 로깅 시스템 개선 (파일 기반 로깅)

### 프로젝트 적용 시점
- **백엔드**: ✅ 완료 (2025-10-15)
- **프론트엔드**: 🔜 예정
- **배포**: 🔜 프론트엔드 수정 후

---

## 참고 자료

### Spring Transaction Management
- [Spring Data JPA - Multiple Datasources](https://spring.io/guides/gs/accessing-data-jpa/)
- [Spring Transaction Management](https://docs.spring.io/spring-framework/reference/data-access/transaction.html)

### RabbitMQ STOMP
- [RabbitMQ STOMP Plugin](https://www.rabbitmq.com/stomp.html)
- [Spring STOMP Broker Relay](https://docs.spring.io/spring-framework/reference/web/websocket/stomp/configure.html)

### 관련 이슈
- GitHub Issue: Neo4j + JPA TransactionManager 충돌
- StackOverflow: RabbitMQ STOMP Invalid Destination

---

**작성자**: Claude AI + 이서인
**검수**: 실제 테스트 완료
**버전**: v1.0
